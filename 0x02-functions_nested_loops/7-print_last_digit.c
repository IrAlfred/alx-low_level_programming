#include "main.h"
#include <stdio.h>

/**
 * print_last_digit - Prints the last digit
 * @n: argument
 * Return: int value
 */
int print_last_digit(int n)
{
	int last;

	last = n % 10;

	return (last);
}
